#include<stdio.h>
#include<string.h>
int main()
{
int s[3], b[3];
for(int i = 0; i < 3; i++){
    scanf("%d", &s[i]);
    b[i] = s[i];
}
int swap;
for(int i = 0; i < 3; i++){
    for(int j = 0; j < 3-1-i; j++){
        if(s[j] > s[j+1]){
            swap = s[j];
            s[j] = s[j+1];
            s[j+1] = swap;
        }
    }
}
for(int i = 0; i < 3; i++){
        printf("%d\n", s[i]);
    }
printf("\n");
for(int i = 0; i < 3; i++){
        printf("%d\n", b[i]);
    }
return 0;
}
